{
  "name": "savepoint-canvas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SavePoint — Canvas-2D pixel-art frontend (parallel demo to the React app). Same backend API.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
